<script setup></script>

<template>
  <div class="divider">
    <div class="sub-divider"></div>
  </div>
</template>

<style scoped lang="less">

  .divider {
    margin: var(--card-divider-margin);
    width: var(--card-divider-width);
    height: var(--card-divider-height);
    background-color: var(--card-divider-bgcolor);
  }

  .sub-divider {
    width: var(--card-sub-divider-width);
    height: var(--card-sub-divider-height);
    background-color: var(--card-sub-divider-bgcolor);
  }
</style>